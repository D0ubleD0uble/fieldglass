//! Nothing here. The package exists for its manifest and its tests: see
//! `tests/standalone.rs`, and the comment at the top of `Cargo.toml` for why
//! the dependency list is what proves anything.
#![forbid(unsafe_code)]
